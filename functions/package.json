{
  "name": "taskvault-functions",
  "description": "TaskVault server-side functions: deposit receipt verification (image) + expiry",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "deploy": "firebase deploy --only functions",
    "shell": "firebase functions:shell"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "private": true
}
